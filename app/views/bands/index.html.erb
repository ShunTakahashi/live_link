<h1>バンド一覧</h1>

<div>
  <%= search_form_for @q do |f| %>

    <%= f.label :name %>
    <%= f.search_field :name_cont %>

    <%= f.submit %>
  <% end %>
</div>
<div>
  <% @bands.each do |band| %>
    <%= link_to band.name, band_path(band) %>
  <% end %>
</div>