<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @live.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @live.content %>
</p>

<%= link_to 'Edit', edit_live_path(@live) %> |
<%= link_to 'Back', lives_path %>